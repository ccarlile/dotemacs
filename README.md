# Chris's emacs config

## To use:

clone this directory to ~/emacs
```
git clone git@github.com:ccarlile/dotemacs ~/emacs
```

symlink `dotemacs.el` to `~/.emacs`
```
ln -s emacs/dotemacs.el ~/.emacs
```
